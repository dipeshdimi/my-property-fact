export const metadata = {
    title: "Welcome to Ludhiana",
    description: "Welcome to Ludhiana properties",
  };
  
  export default function Layout({ children }) {
    return (
        <div>
          {children}
        </div>
    );
  }